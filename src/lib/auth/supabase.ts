/**
 * @deprecated Use `createSupabaseBrowserClient()` from `@/lib/auth/client` in Client Components,
 * or `createSupabaseServerClient()` from `@/lib/auth/server` in Server Components.
 *
 * This module is kept for backwards compatibility.
 */
export { createSupabaseBrowserClient as createClient } from "./client";
